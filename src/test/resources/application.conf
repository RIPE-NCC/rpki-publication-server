locations.rsync.repository-mapping = [
  { "rsync://wombat.example" : "/tmp/a" },
  { "rsync://localcert.ripe.net" : "/tmp/b" },
//  { "rsync://nonexistent.example" : "/nonexistent" },
]

postgresql.url = "jdbc:postgresql://localhost/pubserver_test"
postgresql.url = ${?POSTGRES_URL}

server.address=0.0.0.0
